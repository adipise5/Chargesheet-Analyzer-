# Render free is deliberately a read-only, precomputed snapshot.
APP_ENV=render_demo
APP_HOST=0.0.0.0
DEMO_SNAPSHOT_MODE=true
DEMO_SNAPSHOT_DIR=/app/demo_snapshot
# Render supplies PORT automatically. Do not hardcode it in the service.
